{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build CUDA Program",
            "type": "shell",
            "command": "/usr/bin/nvcc",
            "args": [
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
